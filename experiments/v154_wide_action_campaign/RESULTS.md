# V154 完整五折对照结果（2026-09-09）

## 结论

**本地五折已完成；扩大动作候选范围的两个模型均未通过预先设定门槛。停止本轮 V154 扩池，不生成 V154 比赛 CSV。**

当前真实冠军仍是 V153 p03：P=1049、TP=971、F1=0.927855。本轮没有比赛提交；本地账本剩余6次，平台当前额度因浏览器连接问题未实时核对。0.94尚未达到。

## 同环境配对验证

表内数值为五折合并的代理基线 OOF F1，不是公共榜单 F1，不能把这些增益直接加到0.927855上。共同代理基线 F1=0.857374392。

| 模型 | 16动作预算 | 32动作预算 | 64动作预算 | 32预算正收益折数 |
|---|---:|---:|---:|---:|
| 窄池 CatBoost | 0.858681 | 0.859439 | 0.860905 | 4/5 |
| 窄池 V38式 ET/HGB | 0.859938 | 0.861503 | 0.859887 | 4/5 |
| 宽池 CatBoost | 0.857423 | 0.857236 | 0.858410 | 3/5 |
| 宽池 V38式 ET/HGB | 0.859612 | 0.858221 | 0.854011 | 4/5 |

预算按每546订单归一化；各折按订单数缩放，并只执行模型预计正收益的独立动作，所以实际动作总数不等于表头数字。全部训练、内外层验证和测试采用各自冻结的窄/宽候选规则。模型参数和温度只在内层选择。

门槛固定为：完整5折；32动作预算至少3折正收益；合并 F1 高于代理基线且严格超过同环境两个窄池对照。宽池 CatBoost 甚至未超过代理基线；宽池 V38虽有正收益，但比最强窄池低0.003282。因此没有合格宽池来源，不进入新候选生成与线上方程筛选阶段。这不证明所有扩大候选范围的方法无效，只否定本次冻结生成规则与模型组合。

运行版本：Windows，NumPy 2.5.2、scikit-learn 1.9.0、CatBoost 1.2.10。四个对照使用相同运行时、种子、划分和预算。

## 中断恢复与可追溯性

- 原本地窗口：北京时间14:26:53至20:26:53。
- 19:18后复核发现原控制器与工作进程均已不存在，状态文件却仍为running。最后一次旧日志写入14:55:30，已保留19个模型/折检查点；中断原因未确认。
- 19:20:14在原期限内恢复，未重置窗口。复用19个已有检查点，只训练缺失的第5折宽池V38。
- 19:23:47完成，退出码0，全部20个模型/折组合已有结果。
- 原状态保留在 `local_run_20260909/local_state.before_resume.json`；旧日志为 `training.log`，恢复日志为 `resume.log`。
- 每个冻结输入核对manifest哈希；复用检查点还核对运行版本与manifest哈希。
- 完成结果以 `local_run_20260909/results/progress.json` 的 `status=completed`、`complete=true` 为准。
- 结果SHA-256：`f5150cc4ec089f3242429f1b49956c06cfab4c6c01af8299fd4079717c9f1556`。
- 独立门槛检查产物：`local_assessment/final_gate.json`，`eligible_wide_sources=[]`、`generate_csv=false`。

## 历史账本复核

V153只读audit通过：历史整数约束可行，5份现有CSV校验通过，160个受保护历史文件未变。已计入4次实际提交，剩余6次。没有修改线上分数、没有重置预算、没有上传比赛文件。

旧池待反馈的p05仍保留原文件，不因为V154失败而自动提交。它是增量信息探针，不是本次新模型产物，更不是已证明可冲0.94的文件。现有V153池上限约0.932504，不能把继续拆旧池描述为足够达标。

恢复流程和门槛共8项自动测试通过。这是软件流程验证，与模型效果和线上提分分别计数。

## 云端离线依赖：尚未修复完成

已准备Linux aarch64 / CPython3.10的22个wheel，下载包哈希曾逐个与官方PyPI元数据核对。主要版本为NumPy2.2.6、SciPy1.15.3、scikit-learn1.7.2、CatBoost1.2.8，与本地版本不同；不能将将来的云端结果与本地结果当作同环境配对比较。

离线归档204,179,566字节，SHA-256：`4c73bf79f287fc22d4f5ee23f847a6da66b0d3dc87bfcc1cc64f7f36de1fb9db`。本次再次确认25个本地分片大小和哈希全部正确。

先前网页上传遇到HTTP405，可能留下部分分片，完整上传未确认。此次浏览器连接又出现页面焦点控制超时、新页面连接超时、截图超时，无法读取云端实际状态。因此：

- 不能确认实例仍在运行；不能断言它已停止。
- 离线安装脚本未确认执行，环境不标为ready。
- 没有启动新的云端完整训练，也不延长已经结束的旧云端训练窗口。
- 本地离线包和脚本保留，后续从缺失分片核对开始，不能盲目重传或覆盖现有目录。

## 云端恢复入口

1. 用户在应用中重新打开并确认JupyterLab能正常交互后，先检查现有实例状态和 `/root/work/v154_offline_part_*.bin`。对照本地 `offline_payload/v154_offline_manifest.json` 逐个核对大小与哈希；保留残片，优先只补缺失部分。
2. 确认 `/root/work/install_offline_runtime_20260909.py` 与本地脚本一致，且所有分片完整。该脚本只创建新的隔离环境，使用 `--no-index --require-hashes` 安装，不进行比赛提交。
3. 执行安装前检查 `/root/work/v154_offline_runtime_20260909` 是否已有结果或运行进程，防止重复启动。安装完成必须看到 `install_state.json` 为ready、pip check和小规模模型运行检查成功，才可报告环境可用。
4. 本次V154已被本地配对结果否定，不自动在云端重复完整五折。云端修复是后续不同假设训练的基础设施准备，不是继续此失败路线的依据。

下一阶段应提出与本次扩池不同、可检验的纠错假设，再进行训练；当前没有新的、通过门槛的0.94提交方案，不消耗剩余额度来代替这一证据缺口。
