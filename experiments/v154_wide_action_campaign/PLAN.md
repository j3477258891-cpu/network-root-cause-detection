# V154：扩大纠错覆盖范围，同环境对照，六次自适应测试

> 2026-09-09执行结论：本地完整五折已完成，两个宽池模型均未通过对照门槛，停止本次扩池，不生成V154 CSV。云端离线环境仍待连接恢复与安装确认。详见 `RESULTS.md`；下文保留原计划与旧窗口作追溯，不是新的开训授权。

## 为什么改变方案

真实冠军是V153 p03：P=1049、TP=971、F1=0.927855。V153现有17候选在最新方程下的乐观上限0.932504，不足0.94。V150的80新增中只有20真，继续单独深挖也不足目标。

旧动作生成器只考虑每单低分已选2节点和高分未选2节点。新排序若沿用这个范围，无法发现范围外的错误。V154把两侧上限扩展到4，同时在训练、内层验证、外层验证和测试保持完全一致的生成规则。没有根据测试标签挑候选。

## 本轮训练

- 新的独立六小时窗口：2026-09-08 22:40至2026-09-09 04:40，北京时间；不改写旧窗口。
- 保留V149作为独立动作/方程参考基线，保留已评分p03作为实际冠军。两者不是同一个角色。
- 复用已经隔离站点/模板的五折和交叉拟合节点分数，避免重复节点训练。
- 在同一云端Python/依赖环境、同一种子20260906上，重训四个对照：窄池CatBoost、宽池CatBoost、窄池V38式ET/HGB、宽池V38式ET/HGB。
- 内层仅依据32动作预算收益选择参数，温度仅用内层预测拟合。外层报告16/32/64动作预算的F1、修复和误伤。不能直接将代理基线OOF收益加到真实榜单分数上。
- 宽池来源进入后续候选的门槛：完整五折；32动作汇总收益为正，至少3折正收益，且高于本轮同环境两个窄池对照的最好结果。不以部分训练或小规模运行检查冒充通过。
- 这是一项受控假设检验，不是已证明最优或一定有效的训练方案。使用CPU树模型，不临时移植TabPFN至910B。

## 训练后候选门槛

读取最新所有真实方程（包括V153 p01–p04及之后用户反馈），保护已确认真节点，排除已确认假新增、dd294及既定风险删除池。不得将模型估计当成确定标签。

从合格宽池来源与保留的窄池来源中生成最多48个不同订单动作。核对相对于V149的实际差集，并重新求解整个池的乐观整数上限；若上限不超过0.94，只能标为增量池，不称为冲目标池。未通过宽池训练门槛，不强行扩大线上测试。

以当前冠军为比较标准，固定预测数时还需要13个净TP才能超过0.94（984/1049对应F1=0.940277）；全部为正确新增时至少需要24个（995/1073对应F1=0.940009）。此前25个是足够的数量，但不是最低门槛。这只是数学门槛，不是已找到的动作。

## 剩余六次机会

不重置预算，不自动提交。

1. 最多4次信息探针：在旧池可拆分组与合格新池新组之间，按最多两步前瞻的可兑现最高F1条件期望选择。新组按动作类型区分，每组4–8个独立订单。不能预先承诺四个固定文件。
2. 1次留给实际计数支持的最佳独立组并集。已被实际探针提交过的同一并集不重复上传。
3. 1次用于异常复核；无异常时才考虑有明确模型收益依据的风险版本。

当前已准备的p05（单测候选5）仍有效，可在训练期间由用户测试。若用户测试，必须先从六次中扣除一次，再更新新池预算和方程。禁止把旧池和新池各算六次。

每次分数后先复核TP、文件哈希和方程，再报告当前最高分、最佳已知并集、池上限和继续/合并/停止决定。所有文件交给用户手动提交，每天最多2次。

## 停止与交付

完整训练未通过、超时、无法安装兼容依赖、资源不可用：保存已完成证据，明确报告，不冒充云端训练完成。实际达到0.94才算达标。模型训练结束不自动产生未筛选的比赛CSV。
