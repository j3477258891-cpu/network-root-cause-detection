# V154 离线分片补传交接

用户提供的25个云端SHA-256均已与本地逐MiB前缀或完整分片匹配。第012个分片通过哈希确定为7MiB（早先截图转录误记为6MiB），其余匹配。

- 001–024为正确但不完整的前缀；025完整且SHA一致。
- 只缺42,991,616字节，即41MiB。
- `repair_upload_20260909/`包含24个尾部分片、1个配置、1个修复脚本，共26文件。没有旧的完整分片，不需要覆盖云端已有文件。
- 7项测试通过，包括用全部真实前缀/尾部重建204,179,566字节并验证完整归档SHA，原残片不变；损坏前缀、损坏尾部、归档哈希错误、重复执行均拒绝或安全处理。
- 完整归档SHA：`4c73bf79f287fc22d4f5ee23f847a6da66b0d3dc87bfcc1cc64f7f36de1fb9db`。

## 用户下一步

通过Jupyter文件上传按钮，将上述目录内26个文件上传到现有 `/root/work`（文件浏览器当前显示原分片的目录）。如果出现覆盖提示，先停止核对，不盲目覆盖。

上传完成后可运行：

```bash
python /root/work/repair_offline_parts.py
```

修复脚本先核对原manifest、安装脚本、前缀、尾部以及完整归档，全部一致才创建 `/root/work/v154_repaired_parts_20260909`。保留原分片，在新目录写入完整分片及绑定新目录的安装脚本。不联网、不安装、不训练、不提交比赛。

看到 `repaired_verified_not_installed` 后再考虑安装；出现任何错误先诊断，不删除目录或重复追加。当前尚未执行云端修复，云端安装仍未完成。

本次只准备环境修复。V154模型扩池未通过五折验证的结论、真实冠军与比赛预算均不改变。
