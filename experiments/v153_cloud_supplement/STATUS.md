# 云端补训状态

2026-09-08 22:35（北京时间）核查。

- 用户要求仅交付 CSV 和云端训练，不代交比赛文件。未进行比赛提交。
- 本次曾于17:39:40启动已有 `v25-probe` 环境，成功打开 Jupyter。
- 页面资源为昇腾910B、20核CPU、160GB内存。实际终端预检为 Python3.10.16/aarch64，numpy/torch存在，sklearn/catboost不存在。
- 制作了独立的 `v153_cloud_supplement.zip`（29,333,175字节），SHA-256为 `0ab5b3fa94f09ec922f4d5baec1a75e4b9c641c1d9b0fdd340c1d41629ae22ad`。
- 训练包只包含本项目交叉拟合动作特征、训练标签、评估代码和来源清单。没有凭据，也没有测试真标签。未覆盖V149/V150/V152/V153的冻结候选数据。
- 浏览器选择了该训练包，并确认了28MB大文件上传提示。由于后续标签页失效，未获得上传完成后的文件列表或远程哈希，因此不能认定上传已完整成功。
- 本地便携工作器的小规模运行检查通过；这不是完整五折验证，也不是云端训练结果。
- 没有已确认启动的云端模型训练进程，没有已完成云端模型产物。未执行云端依赖安装。
- 22:32重新查看平台首页，`v25-probe` 已停止，`codexgz` 已锁定，`glm` 已停止。不推断停止原因。
- 原48小时窗口截止2026-09-08 22:06:57+08:00，已经过期。工作器保留此截止时间，未擅自延期。补训续跑需要用户确认新的训练期限。

## 续跑时必须检查

1. 得到新的明确期限后单独记录延期来源，不覆盖旧窗口。
2. 检查远程训练包是否存在及哈希，缺失时重新通过上传入口传输。
3. 在新目录创建独立CPU依赖环境，不将TabPFN临时移植到910B。
4. 检查Python版本与依赖兼容性；运行版本差异要记录，不能将跨版本对照称为完全同环境。
5. 完整五折、内层调参/校准完成后才评价模型；此次只是随机种子互补复验，不保证比原排序更好。
6. 模型输出仍须依据最新真实方程筛选。不得替换已评分V153池或重置剩余6次预算。
