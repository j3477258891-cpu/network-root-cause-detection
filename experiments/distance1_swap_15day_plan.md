# 图结构替换方案：已否决

> 线上结果：`distance1_swap_equation_filtered.csv` 得分 0.903319，TP=939，
> 相对 0.9196729 基线净损失 17 TP。本路线已经否决，以下计划仅保留为历史记录，
> 不再执行。

基线文件：`experiments/v60_combined_checkpoint/highest_verified_combined.csv`（1035 个预测，已知 F1=0.9196729）。

首选提交：`experiments/submissions/distance1_swap_equation_filtered.csv`。

## 提交顺序

| 天数 | 提交 | 目的 |
|---|---|---|
| 1 | 原冠军/校准文件 | 确认当前榜单 TP 基准 |
| 2 | `distance1_swap_equation_filtered.csv` | 验证 40 个一跳上游候选的净增益 |
| 3 | 40 个新增候选拆成 A/B 两半 | 判断哪一半贡献正增益 |
| 4 | 保留正增益半组，拆分剩余半组 | 定位高价值候选 |
| 5 | 合并已确认正标签，提交保守版 | 固定已验证节点 |
| 6–10 | 每天 2 个小批量删/加探测 | 继续解析 TP 差分 |
| 11 | 使用 `v112_adaptive_final_runtime.py` 生成第一版最终提交 | 按真实分数选择删除集合 |
| 12–14 | 对最终删除集合做 2–3 个相邻规模探测 | 防止一次性过删 |
| 15 | 提交当前最高真实 F1 版本 | 锁定结果 |

## 判定规则

- F1 >= 0.945：停止探索，保留该版本。
- 新增组的净 TP > 0：保留该组；净 TP <= 0：回滚。
- 没有真实分数时，不把离线模拟结果写入冠军记录。

## 当前离线证据

- 一跳上游候选训练集根因率约 79.6%，但按测试候选的边数分层估计新增真根因约 30.3/40。
- V11 OOF 中最低分替换节点的命中率约 4%。
- 因此过滤版的期望净增益约 28–29 TP，对应 F1 约 0.946–0.947；该数值仍需真实榜单确认。
